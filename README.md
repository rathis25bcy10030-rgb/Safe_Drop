# Safe_Drop
SafeDrop is a secure file upload portal built with Flask that prevents malicious file uploads using layered validation, file type whitelisting, size restrictions, and double-extension detection. Designed following secure web application best practices
